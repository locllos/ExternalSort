#include <vector>

bool Merge(
  const std::vector<int>& first, 
  const std::vector<int>& second,
  std::vector<int>* result
) {
  return false;
}


bool Sort(
  const std::vector<int>& in,
  std::vector<int>* out
) {
  
}